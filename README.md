The project contains `package.json` with dependencies.
The project contains `.babelrc` with babel configuration.

Project setup
```
npm install
```
Compiles and hot-reloads for development
```
gulp watch
```
Compiles and minifies for production
```
gulp build
```
or
```
gulp
```